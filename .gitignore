# Node (comet CLI 本地安装的副产物)
node_modules/

# Comet 工作流本地文件(项目规定 comet 相关不入库;worktree 经 .worktreeinclude 携带)
/comet.yaml
/package.json
/package-lock.json
/scripts/comet-build-check.sh
/scripts/comet-verify-check.sh

# macOS
.DS_Store

# Xcode / Swift (后续 iPad 工程)
xcuserdata/
*.xcuserstate
DerivedData/
.build/
*.xcworkspace/xcuserdata/

# Agent local installs and runtime state
/.agents/
/.codex/
/.claude/
/.codegraph/
/.comet/
/.mcp.json
/.omx/
/skills-lock.json

# Local workflow / handoff artifacts
/AGENT-HANDOFF.md
/docs/
/openspec/

# git worktree 隔离工作区
.worktrees/

# Xcode / SwiftPM (Task 0 脚手架产物)
ios/**/xcuserdata/
ios/**/.build/
ios/**/DerivedData/
*.xcuserstate
.swiftpm/

# OMC 本地开发环境(运行时状态/notepad/logs,不进仓库)
.omc/

# 本地副产物:截图 / memory 备份等(不进仓库)
*.HEIC
*.tgz

# 会话 transcript 续写导出（勿入库）
*-this-session-is-being-continued-from-a-previous-c.txt
ios/build/

# Superpowers SDD scratch state (worktree-local review packages/progress)
.superpowers/
