import SwiftUI

/// 占位（Task 3）：Task 4 替换为完整 MCP 只读列表 + reload。
struct McpSettingsSectionView: View {
    var body: some View {
        List { Text("settings.mcp") }
            .navigationTitle("settings.mcp")
    }
}
