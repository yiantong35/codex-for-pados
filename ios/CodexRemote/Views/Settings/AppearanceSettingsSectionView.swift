import SwiftUI

/// 外观分区占位（Task 3 填充读改 ThemeManager 的实现）。
struct AppearanceSettingsSectionView: View {
    var body: some View {
        List { Text(verbatim: "appearance placeholder") }
            .navigationTitle("settings.appearance")
    }
}
