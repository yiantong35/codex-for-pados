import SwiftUI

/// 语言分区占位（Task 4 填充读改 LocaleManager 的实现）。
struct LanguageSettingsSectionView: View {
    var body: some View {
        List { Text(verbatim: "language placeholder") }
            .navigationTitle("settings.language")
    }
}
