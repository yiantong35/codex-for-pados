> 说明：本文件为 worktree 内 relay-e2e-spike 探路 change 的验收段；归档合并时并入主库同名文件 `docs/真机验收清单.md`。

# 真机验收清单

## relay-e2e-spike

探路 change：iPad 经阿里云 ECS 上的零知识 relay，端到端加密连回开发机的共享 daemon。对应 delta spec `relay-e2e-transport`。以下逐条真机验收，全部通过方可归档。

前置：ECS 已按 `relay-server/DEPLOY.md` 部署好 relay-server；开发机已起共享 daemon（有可用 control.sock）；iPad 已装带 RelayTransport + 配对导入 UI 的构建。

### 一、relay 服务端（ECS）

- [ ] 真 ECS 起 relay-server（systemd `active (running)`）。
- [ ] `curl http://<ecs-ip>:9000/health`（或 `wss` 前置后 `curl https://relay.example.com/health`）返回 `{"ok":true}`。
- [ ] 安全组 / 防火墙已放行对应端口（9000 或 443），iPad 与开发机均可达。

### 二、开发机拨出（relay-dialout）

- [ ] 开发机跑 `relay-dialout`（正确的 `RELAY_URL` + `CONTROL_SOCK`）拨出成功，终端打印 `relay-dialout 已拨出 ... (role=devMachine)`。
- [ ] 终端打印出配对载荷 `codexrelay://pair?relay=&sid=&pk=&pc=&exp=`（10 分钟内有效）。
- [ ] relay-dialout 桥接的是**开发机现有共享 daemon**（复用 `~/.codex/control.sock`），未自行 spawn 独立 app-server；退出时只停自己 spawn 的 proxy 子进程，desktop GUI 的 app-server 不受影响。

### 三、iPad 配对 + 端到端往返

- [ ] iPad 在配对导入界面粘贴 `codexrelay://pair?...` 载荷，解析成功（relay 地址 / sessionId / 开发机公钥 / pairingCode / 过期时间字段齐全）。
- [ ] iPad 经 RelayTransport 通过 relay 连上开发机（握手完成，建立 SecureSession）。
- [ ] 在 iPad 建一个会话并跑一个 turn：请求发出、响应回来，内容正确往返（无乱码 / 无截断 / 无丢帧）。

### 四、零知识（relay 不可见明文）

- [ ] 在 ECS relay 侧抓包 / 打印经过的帧，确认转发内容是密文（SecureEnvelope），无法还原出 JSON-RPC 明文；relay 只按 sessionId 撮合转发，不解密不解析。

### 五、真同步不被破坏

- [ ] iPad 经 relay 与 desktop 连到**同一个共享 daemon**：iPad 与 desktop 订阅同一 thread，一端产生的 turn / 增量能在另一端看到 live 广播（跨端真同步保持，未被 relay 通道破坏）。

### 六、传输并存（relay 与 SSH）

- [ ] relay 连接与既有 SSH 连接在 `MachineConfig` 中并存；在两种 transport 间切换，各自独立工作、互不干扰（切到 relay 不破坏 SSH 配置，反之亦然）。

### 七、双向认证 + 一次性口令

- [ ] 用**错误 / 无效**的 pairingCode（一次性口令）发起握手，被开发机侧明确拒绝（防冒充：开发机验 iPad 签名 + 校验 pairingCodeProof）。
- [ ] 同一 pairingCode **用过一次后失效**：首次握手成功后，再用同一口令二次握手被拒（`pairingAlreadyUsed`）。
- [ ] iPad 侧验证开发机身份（校验开发机签名 / 公钥），身份不符时拒绝连接。

### 八、错误可见（不静默挂死）

- [ ] 配对信息错误 / 格式非法 / 已过期（`exp` 超时）→ iPad 明确报错，不静默挂死。
- [ ] relay 不可达（ECS 宕 / 端口不通）→ iPad 明确报错，且可重试。

### 九、配对导入 UI 基线（项目级 UI 恒定原则）

- [ ] 配对导入界面在**横屏 / 竖屏**下布局均正常（两个朝向都测，不只测单一朝向）。
- [ ] 软键盘弹出 / 收起时界面无遮挡、无跳变；粘贴 / 输入交互正常。
- [ ] 外接键盘下可正常粘贴 / 编辑 / 提交配对载荷。

### 十、已知限制（记录，非阻塞）

- [ ] （可选）弱网 / 中途断线：当前无自动重传 / 断点续传，断线需手动重连。记录此已知限制，留后续 change 处理。

## relay-live-connection 真机联机验收（待真机）

前置：真 ECS relay 已部署（wss）、开发机 `relay-dialout` 已拨出、iPad 装最新构建。

- [ ] 6.1 开发机跑 `relay-dialout`，终端打印 `codexrelay://pair?…`；iPad 粘贴导入成功建 relay 机器
- [ ] 6.2 iPad 经 relay 连上开发机共享 daemon，建一个会话并跑一个 turn，请求/响应内容正确往返
- [ ] 6.3 传输从 SSH 切到 relay，上层 store/UI 零改照常渲染会话内容
- [ ] 6.4 TOFU：开发机换身份（重生成 DevKeyStore）后再连，iPad 明确报警拒连
- [ ] 6.5 真 wss（TLS）拨出与连接全程加密，relay 只见 SecureEnvelope 密文
- [ ] 6.6 E2E 密钥独立性：relay E2E 身份走独立 Keychain account（与 SSH 密钥分账），删除/重置 SSH 配置不影响 relay 连接，反之亦然；重启 app 后 identity key 保持不变（幂等持久）
- [ ] 6.7 握手失败不挂死：握手中途失败（网络断 / 开发机签名不符 / pairingCode 错）→ iPad 上层明确报错、incoming 流结束，界面不静默转圈挂死（JSONRPCClient 不 hang）
- [ ] 6.8 dialout TLS 证书校验：relay 端用无效 / 自签 / 域名不符证书时，relay-dialout 拒绝连接（fullVerification），不静默降级明文
- [ ] 6.9 TOFU fail-closed（可选，代码级已覆盖）：首次信任写 Keychain 失败时拒绝连接、不 fail-open（真机较难制造，主要靠单测 `testFirstUseFailsClosedWhenPersistFails` 保障，此处仅登记）

## bind-appserver-loopback 安全收口验收（待真机）

前置：开发机装最新 `scripts/start-codex-appserver.sh`。

- [ ] 7.1 默认起 app-server（不设 `CODEX_WS_BIND`），`lsof -iTCP -sTCP:LISTEN -P | grep <PORT>`（或 `ss`）确认仅监听 `127.0.0.1:PORT`、不再对 `0.0.0.0`/`*` 裸绑；同网段另一台主机裸 TCP 连该端口被拒
- [ ] 7.2 逃生阀 `CODEX_WS_BIND=0.0.0.0` 起 app-server，确认监听 `0.0.0.0:PORT`、脚本提示打印「逃生阀裸连，仅供临时排障」警示
- [ ] 7.3 默认 loopback 下 relay 通道仍可用：`relay-dialout` 本机（loopback）连 app-server 成功，iPad 经 relay 跑通 turn（收口不破坏 relay）
- [ ] 7.4 默认 loopback 下 SSH 通道仍可用：iPad 经 SSH 隧道 + `codex app-server proxy --sock` 连通（收口不破坏 SSH）

## relay-pairing-and-resilience 真机联机验收（待真机）

前置：开发机装最新 `relay-dialout`（信任列表 + 二维码 + 撤销 CLI）；iPad 装含相机扫码 + 受信任复连 + 断线重连的构建。真机才能测相机/真弱网。

### 一、扫码配对（相机 UI，模拟器不可测）
- [ ] 8.1 开发机首配模式启动，终端打印 ASCII 二维码（三定位角标可辨）+ `codexrelay://` 明文兜底；iPad「扫码」按钮唤起相机、扫开发机终端二维码 → 解析成功进入配对握手、建立 relay 机器
- [ ] 8.2 iPad 相机权限首次弹窗文案正确（NSCameraUsageDescription 中文）；拒绝权限后「扫码」提示不阻断、手动粘贴入口仍可用完成配对
- [ ] 8.3 扫到非法/过期二维码明确报错不挂死；扫码全屏 cover 横竖屏切换预览铺满不错乱（videoOrientation 目视）
- [ ] 8.3a（#4 串行化）打开扫码页 → 关闭，确认相机指示灯熄灭（无残留运行）；快速进出扫码页多次无卡死、无相机残留（对应 spec「关闭扫码页后相机不残留运行 / 快速开关不产生竞态残留」）

### 二、免配对复连（信任持久化 + 稳定房间）
- [ ] 8.4 首次配对成功后，开发机 `~/.codex-relay-dialout/trusted-ipads.json` 出现该 iPad 记录（0600 权限）；重启开发机进复连模式（终端打印「等待受信任 iPad 复连」、不吐配对载荷），iPad **免扫码/免配对**自动连上、跑通 turn
- [ ] 8.5 一 iPad + 多开发机：iPad 对 A、B 两台开发机各自首配一次后，之后连任一台均免配对（各机器独立稳定房间/信任）
- [ ] 8.6 `relay-dialout --list-trusted` 列出受信任 iPad；`--forget <label>` / `--forget-all` 撤销后，该文件对应记录消失，且 CLI 命令**不吐**一次性配对载荷

### 三、断线自动重连 + resync
- [ ] 8.7 真弱网/切网/短时断网：iPad relay 连接断后自动指数退避重连、重新握手成功，经 `thread/resume` 补齐断线期 desktop 产生的内容，会话一致（不丢不重）
- [ ] 8.8 退避达上限（持续无网）后停止重连、toast「连接失败，请稍后重试」、保留机器；恢复网络后手动重连成功
- [ ] 8.9 失败区分：开发机未开机/房间无对端 → 提示「连接失败」（传输失败，不误报信任撤销）；未受信任 iPad 连到开发机正在服务的房间收到 RejectHello → 提示「已被移除信任，请重新配对」并引导回配对入口

### 四、能耗验证（用户要求项，具体方案后续敲定）
- [ ] 8.10 **能耗验证**（方案待定）：验证断线重连的退避+硬上限不无限空转、app 进后台暂停重连不持续烧电、主动断/trustRevoked 为终态不再重连。可选手段：Xcode Energy/Instruments（Energy Log / Network）观察后台断网时无周期性 radio 唤醒；对比旧 SSH 无界重连的耗电基线。具体验证方案（工具、时长、指标阈值）后续敲定，先登记占位

### 五、安全红线（真机复核）
- [ ] 8.11 E2E 密钥独立于 SSH（独立 Keychain account）；relay 服务端抓包只见密文信封（含 SecureReady 走加密通道，stableSessionId 不明文过 relay）；免配对/复连/重连全程 iPad 与开发机 Ed25519 双向验签未被跳过（改错开发机身份/篡改帧应拒连）

### 六、UI 基线（项目级恒定原则）
- [ ] 8.12 扫码/配对导入界面横竖屏 + 软键盘（不遮挡）+ 外接键盘（可编辑粘贴）均正常

## custom-resizable-columns（自绘可调宽三栏，已归档 2026-07-25，PR #37 合入 master 31c7a1f）

三栏从 `NavigationSplitView + .inspector` 改自绘 HStack + 两条拖拽分隔线。静态布局（三真实列、分隔线、无 gap、右栏渲染无崩）已由快照 PNG 视觉核 + 全量 560/0 单测覆盖；下列为需真机手感 / 端到端确认的动态项。

前置：装最新构建（模拟器已装 `com.tangyujie.codexremote`，真机同构建）；建议连一台真实 daemon 让中栏有重内容（对话）以测掉帧。

### 一、拖拽手感与约束

- [ ] 1.1 拖左分隔线：左栏随手指平滑变宽、**跟手不掉帧**；慢速拖动**无来回抖动**（D2 固定坐标系 + 起点锚差分）。
- [ ] 1.2 拖右分隔线：右栏变宽、跟手不抖。
- [ ] 1.3 **左右完全解耦**：拖左分隔线时右栏宽度纹丝不动；拖右分隔线时左栏纹丝不动。
- [ ] 1.4 任一栏往宽拖到底**停在容器总宽 2/3**，中栏仍保留最小可用宽（不被压没）。
- [ ] 1.5 隐藏把手仍可拖：分隔线无可见把手装饰，但在列缝处拖动列宽随动（透明命中带够宽好抓）。

### 二、横竖屏一致

- [ ] 2.1 竖屏下左栏为**真实列并排挤窄**中/右栏（非系统浮层遮挡）；与横屏行为一致。
- [ ] 2.2 旋转横→竖 / 竖→横：实际渲染宽度随新总宽重算，列宽收敛不溢出、无残留空隙；放宽后恢复用户首选宽度。
- [ ] 2.3 分屏 / Stage Manager 变窄：实际渲染宽度按新总宽收敛，右栏不被挤出屏，且不覆盖持久化的用户首选宽度。
- [ ] 2.4 320pt 紧凑窗口打开左右 overlay：覆盖层不占满窗口，至少保留 44pt 中栏上下文，无水平溢出。

### 三、列宽持久化（D7）

- [ ] 3.1 调整列宽 → 退后台再回前台（进程在）：列宽保持。
- [ ] 3.2 调整列宽 → 杀进程冷启动（或系统回收后重开）：**列宽恢复到上次值**（读 UserDefaults）。
- [ ] 3.3 **窄屏恢复不溢出且不丢首选值**（关键回归）：横屏把侧栏调到 400pt → 缩到 600pt 时仅实际渲染宽度收窄 → 放宽后自动恢复 400pt；窄窗冷启动同样不溢出，持久化首选值保持 400pt。
- [ ] 3.4 **切 tab 列宽各自独立**：机器 A 调宽 → 切机器 B（各记各的）→ 切回 A 恢复 A 的列宽。

### 四、五窗口能力无回归

- [ ] 4.1 右栏三 tab（审查/文件/侧聊）切换 + 「⤢」全屏进出**不崩**（环境注入保留）。
- [ ] 4.2 顶栏按钮（左/下/摘要/右/设置）、摘要浮层、下栏纵向拖高均正常；中等宽度当前显示左栏时点右栏（反向亦然）首次点击即切换目标侧，不需要点击两次。
- [ ] 4.3 顶栏 + 摘要 overlay 迁外层后，safe-area 组合正确（顶栏不被状态栏压、摘要浮层落在内容区）。
- [ ] 4.4 外接键盘：既有 ⌘ 快捷键 / Tab / Esc 正常，不崩不卡；无新增自定义调宽快捷键。

### 五、无障碍（D8）

- [ ] 5.1 VoiceOver 聚焦分隔线读出「调整左栏/右栏宽度」标签。
- [ ] 5.2 VoiceOver 可调节动作（上/下滑）增减列宽，落到 clamp 合法区间。

### 六、中栏重内容性能（真实 daemon）

- [ ] 6.1 中栏为长对话（重内容）时拖分隔线仍跟手不掉帧；若掉帧 → 走 design D3 兜底链（隔离子视图 → onGeometryChange → 拖动降敏），记录并另开修复。

## review-fixes-security-energy（2026-07-30，8 缺陷收口）

### 安全（fail-closed 真机确认）

- [ ] S#1 设置页「隐私」→「允许远端写剪贴板」默认**关**；关闭时远端 `printf '\e]52;c;<base64>\a'` **不改**剪贴板；开启且小内容能写入；开启且 >64KB 内容**拒写**（剪贴板不变）。
- [ ] S#8 dev 侧把 `~/.codex/...identity.key` 手动 `chmod 644` 后重启 dev → 确认被收紧回 `600`；`chown` 到别的用户后重启 → 确认 **fail-closed 拒绝启动**（属主校验分支，单测无法覆盖真实 uid，需真机确认）。
- [ ] S#2 dev 侧模拟信任落盘失败（如临时令 TrustStore 目录不可写）→ 确认会话**未发布**、配对口令**未消费**、iPad 端可重试；恢复可写后重连成功。
- [ ] S#5 iPad 首次生成身份密钥时模拟 Keychain 写失败 → 确认**不缓存**未落盘的密钥、下次启动重新生成而非用脏内存态。

### 能耗（静态结论 + 真机确认）

- 能耗静态结论（已代码走查确认）：
  - #3 空闲会话**无常驻定时器**——移除 30Hz `coalesceTask`，改按需 `flushTask`（`test_idle_no_periodic_wakeups` 覆盖，空闲态 0 唤醒）。
  - #6 后台 / 不可见（`scenePhase != .active`）**不启动轮询**——`startPolling(isVisible:)` 前置守卫。
  - #7 在途首连退后台**即取消**（attempt-token 作废 + take-and-nil close），不再烧到 20s 超时；落 `.disconnected` 终态使回前台可重连。
- [ ] E#4 真机打开/关闭扫码页，相机指示灯正确熄灭、无残留；快速进出多次无卡死、无相机常亮（串行队列 + desiredRunning 消竞态）。
- [ ] E#7 真机首连握手途中切后台 → 再回前台：切后台时连接**不空耗**（不再等 20s）、回前台能正常重连到 `.ready`。
- [ ] E#3 长会话流式输出时观察主线程无 30Hz 空转；停止观察后无残留 flush 任务。

## reconnect-resync

change：iPad 经 relay 断线重连后会话正确恢复。对应 delta spec `ipad-reconnect-resync` + `ipad-approval-handling`。以下逐条真机验收（守 `ui-adaptation-baseline`：横竖屏各验一遍）。

前置：iPad 已装本 change 构建，经 relay 连回开发机共享 daemon；可人为断网（关 Wi-Fi / 断 relay）制造重连。

### 一、item 1 — 审批重放不重复卡

- [ ] 1.1 触发一个审批（如危险命令）→ 审批卡出现 → 人为断网 → 恢复网络重连：同一审批**仍只有一张卡**（无双卡），卡状态回到可操作（`awaitingRecovery` 已清）。
- [ ] 1.2 断线期间**不自动批准/不丢弃**：断网时未决审批卡保留，重连前不被自动放行。
- [ ] 1.3 横屏、竖屏各验一遍 1.1。

### 二、item 2 — 重连收敛（切换即刷新 + 审批全局兜底）

- [ ] 2.1 当前查看的 thread 断线期间产生新内容 → 重连后正文**收敛到最新**，无重复/丢失。
- [ ] 2.2 断线重连后切到一个后台 thread：经 resume 收敛见**最新**内容，不停留在断线前。
- [ ] 2.3 某后台 thread 断线期间有未决审批 → 重连后切过去：**审批已在**（全局通道兜底）且不重复。
- [ ] 2.4 横屏、竖屏各验一遍 2.1/2.2。

### 三、item 3 — 出站离线队列（断线攒、重连按序补发）

- [ ] 3.1 断网期间连发 3 条消息 → 3 条均**乐观回显**、无一丢失 → 恢复网络：**按发出顺序自动逐条发出**，无需手动重发。
- [ ] 3.2 补发**无重复气泡**（仍 3 条用户消息，不因补发再回显一遍）。
- [ ] 3.3 连接已 `.ready` 时正常直发（不入队）。
- [ ] 3.4 断线期间 interrupt/steer、审批回复**不入队**（不随补发被误发）。
- [ ] 3.5 横屏、竖屏各验一遍 3.1。

## relay-reconnect-hardening

change：relay 重连可靠性 11 缺陷收口（#1–#11）。守 `ui-adaptation-baseline`（横竖屏各验一遍）+ `security-first`/`energy-awareness` 红线。

前置：iPad 已装本 change 构建，经 relay 连回开发机共享 daemon；dev 侧 relay-dialout 常驻；可人为断网（关 Wi-Fi / 断 relay）制造重连。

### 一、断网发多条 → 重连按序补发（#3 出站队列 + #1 重握手）

- [ ] R1.1 断网期间连发 3 条消息 → 均乐观回显、无丢失 → 恢复网络：iPad 侧新 ClientHello 经 dev 重握手成功，3 条**按发出顺序**自动逐条发出，无重复气泡。
- [ ] R1.2 弱网多次断连重连：每次都能重新握手到 `.ready`（dev 侧 `hellos!=nil` 不再把新 ClientHello 当迟到 ClientAuth 丢弃）。
- [ ] R1.3 重连有界：持续断网时退避到硬上限后置 `.connectionFailed`（不无界重试、不能耗黑洞）。
- [ ] R1.4 横屏、竖屏各验一遍 R1.1。

### 二、审批重连失败可重试（#6 fail-closed 保留卡）

- [ ] R2.1 触发审批 → 断网 → 在断网态点批准/拒绝：发送失败时审批卡**保留**（不静默消失），可重试。
- [ ] R2.2 恢复网络后重试同一审批：成功送达后卡才移除；期间**不自动放行/不丢弃**。
- [ ] R2.3 横屏、竖屏各验一遍 R2.1。

### 三、后台 running 线程切过去见最新（#3 权威对账 + #7 分页恢复）

- [ ] R3.1 一个后台 thread 断线期间持续产出（running）→ 重连后切过去：正文**收敛到最新**，无重复/丢失。
- [ ] R3.2 会话数超单页（>100）时重连恢复：列表**跟随 nextCursor 翻全部页**，断线前可见的会话重连后仍在（不只剩首页）。
- [ ] R3.3 横屏、竖屏各验一遍 R3.1。

### 四、近上限图片可发不断连（#5 字节预算）

- [ ] R4.1 发一张接近尺寸上限的大图：编码后经加密+base64 装帧**不超过 relay 1MiB 帧上限**，成功送达、**连接不因超帧被断**。
- [ ] R4.2 极端超限图片：被**编码前拒绝**（明确失败）而非发出去撑爆帧导致断连。

### 五、连接健康 / 子进程生命周期（#8/#10/#11）

- [ ] R5.1 half-open（对端静默消失）：心跳探测 10s 内超时判死并触发重连（send 可取消，不再卡死 33min 式挂起）。
- [ ] R5.2 回前台恢复：只触发**单次**探测（不双探、不绕过 missThreshold）。
- [ ] R5.3 dev 侧：iPad TCP reset（无 connectionClose 帧）后，dev 上**无遗留 proxy 子进程**（channelInactive 精确回收自己 spawn 的 PID，未 pkill 误杀 desktop 私有 server）。
- [ ] R5.4 dev 侧：proxy 启动失败时 iPad **明确感知断连**（fail-closed 关连接），而非收 SecureReady 后干等 initialize 超时。
## workspace-ui-review-fixes

change：修复外部 UI 方向 review 的 8 项发现（D1 侧聊状态隔离 / D3 右栏窄宽 / D4 分屏降级 / D5 语言跟随 / D6 删除确认 / D7 触控可及 / D8 空态+滚动锚定）。纯 iOS SwiftUI + Store 层，不触碰 relay/E2E/传输/安全面。以下为**单测/模拟器无法完全覆盖、须真机确认**的项（VoiceOver 语音、真实指针命中、真 Stage Manager/Split View、外接键盘）；模拟器可自验项见构建自验收记录。守 `ui-adaptation-baseline`：涉 UI 项横竖屏各验一遍。

### 一、D7 触控与辅助功能（真机 VoiceOver + 真实指针）

- [ ] 1.1 打开 iOS VoiceOver，逐个聚焦 Composer 五个图标按钮（图片/模型/停止/发送/更多）：每个都朗读**语义标签**（非「按钮」空读），且标记为 button trait 可双击激活。
- [ ] 1.2 VoiceOver 聚焦侧栏会话行：朗读会话标题且作为**按钮**可双击进入（onTapGesture→Button 改造后应有 button trait，非静态文本）。
- [ ] 1.3 用 Apple Pencil / 手指在**最小尺寸**下点按 Composer 图标按钮：命中区 ≥44pt，边缘点按也能触发（`minimumHitTarget44` 实际命中，非仅视觉）。
- [ ] 1.4 横屏、竖屏各验一遍 1.1/1.3。

### 二、D3 + D4 窄窗 / 分屏（真 Stage Manager + 真 Split View）

- [ ] 2.1 真机开 Split View 把本 app 拉到**最窄档**（约 320pt 级）：右栏三个 tab（review/files/sideChat）+ 全屏入口**全部可见可点**，无被裁掉的入口（P1#2 回归位）。
- [ ] 2.2 真机 Stage Manager 自由缩放窗口到 <668pt：三栏自动降级（先收右栏，再不够收左栏），**无横向溢出/内容被切**，中栏始终完整；放大回去后被收起的栏可重新展开、列宽保留（D4 决策 A：自动收起而非叠加浮层）。
- [ ] 2.3 横屏、竖屏 + Split View 左右两侧位置各验一遍 2.1/2.2。

### 三、D5 应用内语言切换（真机视觉核对中英）

- [ ] 3.1 应用内切换到英文：FileBrowser / 审查模式名（ReviewPanelTypes）/ 快捷键设置 / 右栏 tab label 等**全部英文**，无中英混排；切回中文亦然（跟随注入 locale，非仅系统语言）。
- [ ] 3.2 横屏、竖屏各验一遍 3.1。

### 四、D6 删除配对机器二次确认（真机交互）

- [ ] 4.1 长按/进入机器管理菜单点「移除」：弹出 destructive `confirmationDialog` 二次确认；取消则机器与连接保留，确认才永久移除并断连。
- [ ] 4.2 管理菜单连接/断开按钮按连接态互斥（已连显断开、未连显连接），无「同显连接+断开」。

### 五、D1 侧聊 / 主对话状态隔离（真机交互）

- [ ] 5.1 主对话进行中打开侧聊（side chat）：侧聊活动**不覆盖/不清空**主对话的审查状态（右栏 diff/审查入口仍指向主对话）；关闭侧聊后主对话状态完好（P1#1 回归位，relay-only 路径）。
- [ ] 5.2 主对话与侧聊分别属于不同 thread，断线重连后各自 rejoin 恢复，互不串台。

### 六、D8 空态 + 滚动锚定（真机手势）

- [ ] 6.1 未选任何会话：主区显示 `split.selectConversation` 引导空态（非纯空白背景）。
- [ ] 6.2 对话滚到接近底部时新消息到达 → **自动滚到底**；手动上滑离开底部时新消息到达 → **不自动拉回**，出现「新消息」浮标，点按平滑滚到底并消失（无常驻定时器/轮询）。
- [ ] 6.3 外接键盘接入：滚动/浮标点按与软键盘弹出/收起下布局稳定；横屏、竖屏各验一遍 6.2。

## land-connection-health（连接健康可见性重落地，已归档 2026-08-04，PR #48 squash 合入 origin/master 12090a2f）

change：在无-SSH master 基线上语义移植重新落地 #45 的连接健康可见性（端到端心跳判死 / relay 对端离开提示非判决 / 断线 UI 三态横幅 + tab 灰点）。对应 capability `ipad-connection-health`。逻辑与状态→视觉映射已由单测覆盖（`HeartbeatMonitorTests` / `ConnectionStoreTests`（心跳判死、peer-left probe 命中留 ready、probe miss 触发重连）/ `ConnectionBannerStateTests` / tab 灰点正交 / `RelayReconnectTests`），横竖屏 + 键盘空态布局已离线截图核；下列为需**真机 live relay + 人为制造断线**才能眼见为实的端到端观感项（守 `ui-adaptation-baseline`：横竖屏各验一遍）。

前置：真 ECS relay 已部署（wss）、开发机 `relay-dialout` 已拨出、iPad 装本 change 最新构建并经 relay 连上开发机（连接已入 `.ready`）。

### 一、断线判死 → 可见异常态

- [ ] 9.1 连上后人为断 relay（关 Wi-Fi / 断网 / 停 relay-server）：端到端心跳连续错过 2 次（≈20–30s）后 ConnectionStore 判死离开 `.ready`，**不再停留 ready 直到 relay idle 超时**；界面出现断开提示（非静默）。
- [ ] 9.2 底层 ws 直接断开（非心跳超时路径，如 relay-server 主动断）→ 同样收敛到可见异常态。
- [ ] 9.3 有界重连耗尽（持续无网，退避达上限）→ 落 `.failed`，横幅可见、可手动重连。

### 二、断线 UI 三态横幅 + tab 灰点（真断线态触发）

- [ ] 9.4 `.reconnecting`（重连中）：黄色横幅，文案正确；重连成功后横幅自动消失、回 `.ready`。
- [ ] 9.5 `.failed`（重连耗尽）：红色横幅 + 「重新连接」入口，点击真的重新发起连接并能连回。
- [ ] 9.6 `.trustRevoked`（收到 RejectHello / 信任撤销）：红色横幅 + 「重新配对」入口，引导回配对，与 `.failed` 文案/动作区分正确。
- [ ] 9.7 tab 灰点：连接异常时对应机器 tab 显示灰点；恢复 `.ready` 后灰点消失。
- [ ] 9.8 **红灰正交**（关键回归）：灰点仅在连接异常时亮（`.ready` 前提下才可能有 systemError 红点）；构造「连接正常但有 systemError」与「连接异常」两态，确认红点 / 灰点互不误显、语义不串。

### 三、peer-left 提示非判决（防降级红线，真机复核）

- [ ] 9.9 开发机侧对端正常离开（relay-server 发 peer-left 信令）→ iPad **不直接判死**，仅触发 `probeOnce()` 核实：若探活仍通（对端其实在）连接**保持 `.ready`**、不误弹横幅 / 不误亮灰点；只有 probe 也失败或心跳判死才进异常态（恶意 / 抖动的 relay 提示不能凭空杀健康连接）。

### 四、能耗 + UI 基线（项目级恒定原则）

- [ ] 9.10 心跳前后台门控：app 切后台时心跳暂停（不持续烧电 / 无周期性 radio 唤醒）、回前台恢复探活；断线重连走有界退避（封顶）不无限空转。
- [ ] 9.11 断线横幅 / 灰点 / 重连入口在**横屏 / 竖屏**下均正常显示与交互；软键盘弹出、外接键盘下不遮挡、可操作重连 / 重新配对入口。

## security-energy-review-fixes-2（2026-08-06）

change：安全与 iPad 能耗复审第二轮收口。自动化结果见 `docs/superpowers/reports/2026-08-06-security-energy-review-fixes-2-verify.md`。

### 一、配置升级与连接意图

- [ ] SE2.1 从上一版安装直接升级：嵌套 relay、旧 pairing 和当前格式机器均保留；混入旧 SSH / 损坏项时只过滤无效项，当前活跃机器正确回落。
- [ ] SE2.2 在机器菜单显式断开后，切到别的 tab 再切回、app 退后台再回前台均不自动重连；点击“连接”后只建立一条连接。

### 二、relay 安全与多 tab 能耗

- [ ] SE2.3 向当前连接注入其它 sessionId 的 peer-left，连接保持 `.ready` 且不额外探活；当前 session 的突发 peer-left 只合并成一次加速探测。
- [ ] SE2.4 同时缓存多个机器 tab：活动 tab 心跳约 10s，非活动 tab 约 60s；切回活动 tab 只补一次立即探测，app 后台全部暂停。
- [ ] SE2.5 Instruments Energy Log 观察空闲 10 分钟：项目列表每 30s 只请求首页，失败逐步退避，历史页数增加不放大每轮请求量。

### 三、dialout 子进程回收

- [ ] SE2.6 dev 侧断开 relay / TCP reset 时，NIO EventLoop 仍可及时处理其它事件；proxy 正常响应 SIGTERM 后被 reap，无僵尸。
- [ ] SE2.7 使用忽略 SIGTERM 的 proxy stub：宽限期后仅该子 PID 收到 SIGKILL 并被 reap；desktop 私有 app-server 不受影响。

## functionality-review-fixes（2026-08-06，功能实现全面收口）

前置：iPad 安装本 change 构建，开发机运行 relay-server + relay-dialout + Codex app-server；以下项目均未在自动验证中冒充真机通过。

### 一、Relay 与连接生命周期

- [ ] F1.1 发送接近 1 MiB wire limit 的文本/图片，成功送达；最终帧超限时发送前明确失败。
- [ ] F1.2 relay-server 中断后恢复，dialout 自动退避重拨；SIGINT/SIGTERM 后无遗留 bridge 子进程。
- [ ] F1.3 用户点 Disconnect 后切 tab、退后台/回前台均保持断开；只有显式 Connect 才重连。

### 二、Server Request 与审批

- [ ] F2.1 requestUserInput 的多问题、options、自由输入和 secret 均可填写，提交/取消 wire response 正确。
- [ ] F2.2 MCP URL 与表单 elicitation 各字段可提交，未知嵌套 schema 显式拒绝。
- [ ] F2.3 command/file/permissions 审批完整展示上下文，批准、拒绝及 turn/session scope 符合选择。

### 三、恢复、侧聊与滚动

- [ ] F3.1 running turn 断线恢复后继续显示进行中；终态按序 drain outbox，无重复消息。
- [ ] F3.2 侧聊与主对话的内容、resume、审批和审查上下文互不覆盖。
- [ ] F3.3 内容增长时近底自动跟随，离底不抢滚动；点“新消息”到真实 bottom sentinel。
- [ ] F3.4 切 cwd/thread/连接时 full diff 失效刷新，手动刷新及全量审查使用最新上下文。

### 四、终端、图片与交互覆盖

- [ ] F4.1 终端退出后可在同 cwd 重启，terminate/切 cwd/断线无旧进程串台。
- [ ] F4.2 快速选择图片 A/B、编码中删除/发送/离页时，迟到结果不会覆盖或复活附件。
- [ ] F4.3 横屏、竖屏、Split View、VoiceOver 与外接键盘覆盖 F2-F4，无重叠、裁切或焦点陷阱。
